# دفتري

## قاعدة ثابتة: المرفوع يبقى كما هو

كل ما ترفعه الأم — جدول الحصص، الواجبات، الاختبارات، المستلزمات، التسميع —
يُحفظ **كما هو مكتوب بالصورة بالضبط**. ممنوع على البرنامج:

- اختراع أو تخمين أي قيمة غير واضحة بالصورة (الحقل غير الواضح يبقى فارغاً).
- نسخ قيمة من خلية إلى خلية ثانية لأنها «تبدو متشابهة».
- تصحيح أو توحيد أو اختصار أسماء المواد والمعلمات ونصوص التفاصيل.
- إضافة أي كلمة من عند البرنامج داخل حقول الأم (مثل لصق ملاحظة بحقل
  التفاصيل).
- حذف أي عنصر مقروء من الصورة — بما فيه التكرار: نفس المادة قد تجي حصتين
  متتاليتين أو كل يوم، وهذا صحيح ويُنقل كما هو.
- تغيير صيغة ما كُتب (مثل توحيد "12.45" إلى "12:45").

الاستثناء الوحيد هو ما تفرضه قيود قاعدة البيانات، لأن البديل عنه خسارة
الصف كاملاً أو فشل الرفعة كلها:

| القيد | ما نضطر له |
|---|---|
| `class_schedule.day` CHECK (خمسة أسماء) | توحيد صيغة اسم اليوم ("الإثنين" ← "الاثنين") |
| `class_schedule` UNIQUE(child_id, day, period_number) | خانة واحدة لكل (يوم + رقم حصة) |
| `class_schedule.period_number` CHECK (1–12) | تجاهل رقم حصة خارج المدى |
| `tasks.type` CHECK (واجب/حفظ/اختبار/مشروع/درس) | نوع غير معروف يصير "واجب" |
| `memorization.kind` CHECK (آية/حديث) | ما ليس "حديث" يصير "آية" |

أي إضافة جديدة تمسّ المرفوع لازم تدخل هذا الجدول أو ما تُكتب أصلاً.

### ما ليس من المرفوع

مسمّيات البرنامج الثابتة ليست من بيانات الأم ولا تدخل تحت القاعدة: أسماء
أيام الأسبوع، وأرقام الحصص («الحصة الأولى»...)، وصيغ عرض الوقت والتاريخ.
هذي بنية البرنامج. الممنوع هو المساس بما **داخل** الخلية: المادة، المعلّم/ـة،
نص الواجب، وترتيب الحصص كما هي بالصورة.

والترتيب بالعرض بنيوي فقط: الحصص حسب `period_number`، والمهام حسب
`due_date` — أي حسب ما جاء بالصورة نفسها، بلا إعادة ترتيب.

### التعديل اليدوي بعد الرفع لا يخالف القاعدة

القاعدة تمنع البرنامج من تخمين أو تصحيح ما بالصورة **تلقائياً وبصمت**. أما
تعديل الأم نفسها لعنصر موجود — لتصحيح خطأ بالاستخراج أو تحديث تغيّر لاحقاً
— فحق دائم ومتاح بزر «تعديل» على كل عنصر مرفوع تقريباً: خانة بجدول الحصص
(مادة/معلّم/وقت — `PATCH`/`POST`/`DELETE /api/class-schedule[/id]`، اليوم
ورقم الحصة ثابتان لأنهما يحددان موقع الخانة)، واجب أو اختبار
(مادة/نوع/تفاصيل/تاريخ — `PATCH /api/tasks/[id]`، من `TaskModal`)، مستلزم
(اسم/تاريخ — `PATCH /api/requirements/[id]`)، وعنصر حفظ (نوع آية أو حديث/
مرجع/تفاصيل — `PATCH /api/memorization/[id]`، من الخطة الأسبوعية
بالرئيسية بعد ما انشال تبويب الحفظ). كل تعديل بفعل صريح من الأم على عنصر تعرفه بعينه، لا تخمين
عن عنصر غامض — والفرق هو جوهر القاعدة.

## الخطة الأسبوعية كقائمة إنجاز

`WeekPlanPanel` هي **الواجهة الرئيسية** (تبويب الرئيسية) — مع أكثر من طالب/ة
يظهر شريط تبديل `ChildSwitcher` والاختيار محفوظ بـ`daftary_plan_child`. كل واجب بسطر مع
تفاصيله الحرفية وعلامة ✓، مجمّعة باليوم، مع نسبة الإنجاز والفلاتر. «تم»
تحوّل `tasks.status` إلى `done` (لا `archived`) فيبقى الواجب ظاهراً
بالقائمة ويُحتسب بالنسبة ويمكن التراجع عنه (`done: false`). الرئيسية تعرض
النشط فقط. مطابقة إعادة الرفع تشمل `done` كذلك حتى لا يتكرر الواجب.

إضافة يدوية (بلا صورة) متاحة من نفس الشاشة — زر «+ واجب» بأعلى بطاقة
الطالب/ة يفتح نموذج مادة + نوع (واجب/اختبار/مشروع/حفظ، بلا «درس» لأنه
يُستخرج من الصور فقط) + تفاصيل + تاريخ (`POST /api/tasks`،
`app/api/tasks/route.js`). يدخل بـ`source: "manual"` بنفس جدول `tasks`
فيرتّبه الخطة الأسبوعية بنفس منطق المستخرج من صورة ويأخذ تذكيره العادي
من كرون `/api/cron/reminders` تلقائياً بلا أي كود إضافي — قبل الموعد
بيوم ويوم الموعد، للواجب والاختبار سواء. هذا فرق عن قاعدة «المرفوع يبقى
كما هو»: ما فيه صورة تُقرأ هنا، فالأم تكتب البيانات مباشرة بلا استخراج.

كل رفعة خطة ناجحة تنظّف المنجز القديم لنفس الطالب/ة (قرار ١٦ سبتمبر —
كان يتراكم بالرئيسية للأبد): تُحذف نهائياً مهام `tasks` بحالة `done`
بلا تاريخ استحقاق أو بتاريخ قبل أحد هذا الأسبوع، وكل `memorization`
بحالة `done=true` (ما له تاريخ استحقاق أصلاً). **غير المنجز يبقى كما هو
مهما قدم** — الحذف للمكتمل فقط، حتى ما تضيع مهمة الأم لسه ما خلصتها.
(`cleanupCompletedBeforeThisWeek`, `lib/planApply.js` — يشتغل عند اعتماد
المسودة، لا عند التحليل.)

## التذكيرات: قبل الموعد بيوم

قاعدة واحدة تنطبق على جميع المشتركين — أي شيء له موعد يوصل تذكيره **قبله
بيوم**، إضافةً للتذكير يوم الموعد:

| النوع | قبل بيوم | يوم الموعد |
|---|---|---|
| اختبار | `exam_day_before` | `exam_today` |
| واجب / مشروع / حفظ | `task_day_before` | `task_today` |
| مستلزمات | `requirement_day_before` (مجمّعة لكل أم) | — |
| تسميع **له موعد** (`memorization.recite_on`) | `recitation_day_before` (مجمّعة) | `recitation_today` (مجمّعة) |
| تسميع **بلا موعد** | السبت والثلاثاء، مجمّعة | — |
| متابعة يومية (`evening_nudge`) | كل يوم ٣ العصر بتوقيت الكويت، رسالة واحدة لكل أم، كرون `/api/cron/evening` | — |
| **درس** (محتوى المنهج بالخطة الأسبوعية) | **لا تذكير أبداً** — يُعرض بقسم «دروس الأسبوع» فقط | — |

قرار ١٦ سبتمبر: التذكير للواجب والاختبار والمشروع والحفظ فقط؛ محتوى المنهج
(بنود، صفحات، مواضيع، «الواجب ما تكلف به المعلمة») يُستخرج بنوع `درس`.
وتذكيرات الواجبات **مجمّعة**: إشعار واحد لكل أم لكل نوع (مثلاً «غداً موعد
تسليم ٥ واجبات لـ لولو: …») مع صف `reminder_log` لكل واجب.

التاريخ يُقرأ من `due_date` كما استُخرج من الصورة، بلا أي تقديم أو تأخير.

موعد المهمة بالخطة الأسبوعية (`upload-schedule`) بهذا الترتيب: تاريخ صريح
بالصورة ← اسم يوم (يُحسب بقاعدة «كل أسبوع بأسبوعه») ← **بلا يوم داخل خطة
أسبوعية: آخر يوم بمدى الخطة (الخميس)** — من المدى المكتوب بالصورة إن وُجد
وإلا خميس أسبوع الخطة (قرار صاحبة التطبيق ١٥ سبتمبر) ← عبارة نسبية
(«نهاية الفصل») تبقى بلا موعد.
و`reminder_log` يمنع التكرار: بـ UNIQUE(task_id, kind) لما يكون فيه مهمة،
وبفحص (mother_id + kind + تاريخ اليوم) للرسائل المجمّعة.

## الاشتراك: تجربة مجانية محدودة لمرة واحدة (لا فترة زمنية)

قرار ١٥ سبتمبر ٢٠٢٦ (إطلاق الحملة) كان: الاشتراك مُلزم من أول دخول بلا أي
تجربة. قرار ١٦ سبتمبر ٢٠٢٦ **عدّله**: بعد يوم واحد من الإطلاق، ٢١ تسجيلاً
انحجبوا حتى عن إضافة أول طالب — فصارت التجربة تسمح لكل أم (جديدة أو
سابقة، بأثر رجعي) بمجاناً ومرة واحدة، **بلا أي حد زمني** (تنتهي باستهلاك
الحد لا بمرور وقت):
- إضافة **طالب واحد** فقط (`childAddAllowedByTrial`، `lib/appEntitlements.js`).
- تحليل **جدول حصص واحد** ناجح (`upload-class-schedule`).
- تحليل **خطة أسبوعية واحدة** ناجحة (`upload-schedule`).
- أسئلة المعلم الذكي التجريبية (١٠ أسئلة) — ميزة مستقلة أصلاً بـ`lib/plans.js`،
  ما تأثرت بهذا القرار.

الاستهلاك يحصل **فقط عند النجاح الفعلي** (طالب أُضيف، تحليل انحفظ) — وبعد
دخول شاشة المراجعة (١٨ سبتمبر) صار استهلاك الخطة **عند اعتماد المسودة** لا
عند التحليل، فنتيجة ترفضها الأم ما تخسّرها التجربة. ولهذا `isTrialExempt`
تستثني `POST`/`DELETE` على `/api/upload-drafts/<id>` — بدونها تتحلّل الصورة
وما تقدر تحفظ نتيجتها، فتصير التجربة بلا فائدة.
محاولة فاشلة (صورة غير واضحة) ما تخسّر التجربة، فتقدر الأم تعيد المحاولة.
`mothers.trial_schedule_used` و`trial_plan_used` (افتراضياً `false`) يمنعان
التكرار؛ عدد الطلاب الحالي (٠) يحدد أهلية طالب مجاني. `isTrialExempt`
بـ`middleware.js` هي نقطة التفعيل الوحيدة — أي مسار رفع جديد يحتاج تجربة
مشابهة يُضاف هناك صراحة، ما يفترضها تلقائياً.

بعد استهلاك التجربة، الاشتراك مُلزم كالمعتاد: `APP_PAYWALL_ENABLED=true`
هو المفتاح الوحيد، ولا تاريخ إلزام عام ولا مدة سماح إضافية بالكود.

## العائلة: اشتراك واحد لوليّي أمر (قرار ١٨ سبتمبر)

الاشتراك صار للعائلة لا للحساب: الأم تدعو **ولي أمر ثانٍ واحد** (الأب)
برقم جواله، وينضم بحسابه هو فيشوف نفس الأبناء والواجبات ويوصله نفس
التذكيرات — بلا اشتراك جديد وبلا مشاركة كلمة سر.

**البنية.** جدول `families`، و`mothers.family_id` + `family_role`
(`primary` | `secondary`)، و`children.family_id`. كل حساب — قديم أو
جديد — له عائلة: زناد `ensure_family()` (BEFORE INSERT) ينشئها تلقائياً،
والترحيل رجّع ١٤٧ أماً إلى ١٤٧ عائلة و١٨٣ ابناً بلا يتيم واحد. يعني
الحساب الفردي **هو** عائلة من شخص واحد، فما فيه مسار ثانٍ بالكود يعامله
معاملة خاصة.

**الملكية بالمسارات.** ما عاد فيه `.eq("mother_id", motherId)` بمسارات
البيانات — كلها تمر بـ`lib/family.js`: `childInFamily` للطالب،
`rowInFamily` لصف مرتبط بطالب، `familyChildIds` للقوائم. أي مسار جديد
يلمس بيانات طالب لازم يستخدمها، وإلا الأب ما يشوف شيئاً (أو — الأسوأ —
تنكسر عزلة العائلات.)

**حدود مفروضة بالقاعدة لا بالواجهة** (فهارس فريدة جزئية): عائلة وحدة ما
تقبل أكثر من `primary`، ولا أكثر من `secondary` — فمحاولة ولي أمر ثالث
تُرفض من القاعدة حتى لو انحايل على الـAPI. والانضمام بقبول صريح من
المدعوّ نفسه (`family_invites` + لافتة بالتطبيق)، ما ينضاف أحد بلا علمه.

**حساب عنده أبناء ما ينضم** (قرار صاحبة التطبيق): ما فيه دمج عائلتين —
`POST /api/family` يرد 409 ويقول له يمسح حسابه ويدخل من جديد. الدمج
يعني قرارات صامتة عن بيانات موجودة، وهذا ضد قاعدة «المرفوع يبقى كما هو».

**الاشتراك والتجربة على مستوى العائلة**: اشتراك **الأساسي** هو اشتراك
العائلة، وعدد الطلاب يُحسب بـ`family_id`، والتجربة المجانية تُستهلك
للعائلة كلها (`family.some(...)`) — وإلا صار كل أب ينضم يجيب تجربة
جديدة مجاناً. `hasAppAccess` ترجّع `familyRole` كذلك.

**التذكيرات تتوزّع على الاثنين**: `loadFamilyParents` + `deliverToFamily`
بكرون التذكيرات والمساء. القيد بـ`reminder_log` تغيّر من
`UNIQUE(task_id, kind)` إلى `UNIQUE(task_id, kind, mother_id)` — بدونه
سجل الأم كان يمنع وصول التذكير للأب بصمت. ما فيه مفتاح لكل ولي أمر:
الاثنان يوصلهما نفس الشي.

**حذف الحساب يفرّق بالدور**: الثاني يطلع وأبناؤه يبقون عند الأساسي؛
والأساسي يحلّ العائلة ويُخرج الثاني لعائلة جديدة خاصة به قبل حذف
الأبناء. وإخراج الثاني (`DELETE /api/family`) يقطع وصوله فوراً.

**`/api/family` معفى من حجب الاشتراك** (`APP_PAYWALL_EXEMPT_PREFIXES`):
الأب المدعوّ ما عنده وصول قبل ما ينضم، فحجب المسار يخلي القبول مستحيلاً.

## ملاحظات المعلم (قرار ١٨ سبتمبر)

قسم «الحفظ» المستقل داخل تبويب «المتابعة والدرجات» (كان اسمه «الحفظ
والدرجات») انشال وحلّت محله
**ملاحظات المعلم**: ما تسمعه الأم باجتماع أولياء الأمور تكتبه هنا بمادته
وفترته، فيتجمّع بالفترات وتتابع تطوّر الطالب على مدار السنة.

**الحفظ ما تأثر**: يبقى ظاهراً بالخطة الأسبوعية بالرئيسية مع علامة
الإنجاز وزر التعديل، ويُستخرج من صور الخطة كما هو، وتذكيراته شغالة —
التبويب كان يكرّر ما هو معروض أصلاً. (`MemorizationSection` انحذفت،
و`WeekPlanPanel` هي العارض الوحيد الحين.)

- جدول `teacher_notes` (child_id, subject, note, term, created_at)،
  و`term` نص حر: قائمة جاهزة (الفترة الأولى…الرابعة) أو تسمية تكتبها
  الأم بنفسها.
- قائمة المواد تجي من **جدول حصص الطالب/ة نفسه** بلا تكرار، لا من قائمة
  ثابتة عندنا — فالأم تلقى مواد مدرستها هي. وفيه خيار «مادة ثانية».
- العرض مجمّع بالفترة ومرتّب **بتسلسل الفترات** (الأولى ← الرابعة) لا
  بالأحدث، لأن القراءة من فوق لتحت هي نفسها متابعة التطوّر.
- كلها كتابة يدوية بلا استخراج من صورة، فقاعدة «المرفوع يبقى كما هو»
  ما تخصّها — مثل الإضافة اليدوية للواجبات.
- `reset-year` و`delete-account` يشملانها.

وخانة **الدرجات** أخذت نفس التفصيلين (قرار ١٨ سبتمبر): المادة قائمة من
جدول حصص الطالب/ة + خيار كتابة مادة ثانية، والفترة بنفس قائمة الملاحظات
أو تسمية تكتبها الأم. تنحفظ بعمود `exam_grades.period` (نص) وتُعرض بسطر
الدرجة. العمود القديم `term` (عدد ١ أو ٢ يُحسب من الشهر) ما يُقرأ بأي
مكان — تُرك كما هو ولا يُبنى عليه شيء جديد.

## أشرطة الشرح الصفراء تنقفل بـ✕ (قرار ١٨ سبتمبر)

الشريط الأصفر اللي يشرح فكرة الشاشة مفيد أول مرة ويصير حشواً بعدها.
`HintBanner` (`app/page.jsx`) يعطيه ✕، والقرار محفوظ بالجهاز
(`daftary_hint_<المفتاح>`) لكل شريط على حدة — تقفلين شرح المعلم الذكي
ويبقى شرح ملاحظات المعلم ظاهراً. المطبَّق عليها: شاشة الرفع (مفتاحها
مسارها)، المعلم الذكي، ملاحظات المعلم، وشريط الأذونات.

شريط الأذونات يحفظ مع الإقفال **وش كان ناقصاً** وقتها، فلو تغيّر الناقص
بعدين (مثلاً الصور انمنعت وهي مسموحة قبل) يرجع يظهر — ما نخفي عنها سبباً
جديداً لتعطّل الرفع أو التذكيرات.

**رسائل الحالة تبقى بلا ✕**: «خلص رصيد هذا الطالب»، «باقتك ما تغطي عدد
أبنائك»، تنبيه اختلاف الصف. إخفاؤها يخفي مشكلة قائمة لازم تعرفها.

## تبويبات الشريط السفلي: «الطلبات والجداول» مدموج

قرار صاحبة التطبيق: تبويب «المتطلبات» المستقل انضم داخل تبويب «جدول
الحصص» بتبويب واحد اسمه «الطلبات والجداول» (`TABS` بـ`app/page.jsx`،
المفتاح بقي `"schedule"` — الأيقونات الحالية تعتمد عليه). داخل الشاشة
مفتاح تبديل (`scheduleSubTab`، قسمين: «الجداول» و«الطلبات») يعرض جدول
الحصص أو قائمة المستلزمات (`RequirementsCard`) — نفس المكوّنات والمنطق،
بس بشاشة وحدة بدل تبويبين. الهدف تفريغ خانة بالشريط السفلي (كان ٥
تبويبات صارت ٤) لميزة **المشاريع** المستقبلية اللي بتاخذ اسم «الطلبات»
لاحقاً — لسه ما نُفّذت، مجرد نية معلنة وقت هذا القرار.

## قراءة الصور

الجداول المدرسية غالباً مطبوعة بالعرض فتنرفع مقلوبة ٩٠ درجة، والنص العربي
المقلوب لا يُقرأ بعد التصغير — وهذا يدفع النموذج للتخمين. لذلك:

- `lib/visionExtract.js` يفحص الاتجاه بنداء مستقل صغير (Haiku) قبل القراءة —
  لأن نموذج القراءة لما يُطلب منه «اقرأ» يقرأ المقلوب ويخمّن (شوهد
  بالإنتاج). الصورة المائلة تُرجع `needsRotation` والجهاز يدوّرها ويعيد
  الإرسال (لين ٣ محاولات). وقاعدة الاتجاه داخل أمر القراءة تبقى طبقة ثانية.
  الصورة غير الواضحة تُرجع `{"unreadable": true}` فنرفض بلا ما نمسّ بيانات
  الأم.
- الصور تُرسل بضلع ١٥٦٨ بكسل (أقصى ما تستفيد منه واجهة التحليل). أي تصغير
  تحته يمحي أسماء المعلمات الصغيرة.
- لا يُمسح الجدول القديم إلا بعد نجاح إدخال الجديد.
- **الخطة الأسبوعية لا تُحفظ مباشرة** (قرار ١٨ سبتمبر): نتيجة التحليل تدخل
  `upload_drafts` كمسودة، وتعرضها شاشة `ReviewDraftScreen` بعدد العناصر
  الفعلي وكل بند قابل للتعديل أو الحذف، وما ينحفظ شي بجداول الطالب/ة إلا
  باعتماد الأم (`POST /api/upload-drafts/[id]`، منطق الحفظ بـ`lib/planApply.js`).
  رفعة جديدة لنفس الطالب/ة تلغي المسودة المعلّقة قبلها. **جدول الحصص
  يبقى يُحفظ مباشرة** — شبكة ٥×٨ ما تنفع للمراجعة سطراً سطراً (قرارها).
- **النص الأصلي يبقى**: كل بند يُحفظ معه `source_text` — النص كما ظهر
  بالصورة حرفياً، جنب البيانات المنظّمة لا بدلاً عنها. يُعرض بشاشة
  المراجعة وبتفاصيل الواجب («النص كما جاء بالصورة») لتقارن الأم بنفسها.
- **درجة الثقة**: النموذج يرجّع لكل بند `confidence` للمادة والنوع والموعد
  بثلاث قيم (`confirmed`/`inferred`/`unclear`)، و«unclear» تطلع بعلامة
  تنبيه بشاشة المراجعة لتحددها الأم — بدل ما يخمّنها البرنامج. العلامة
  ترتفع أول ما تعدّل الأم البند.
- **الصورة الأصلية تبقى أسبوعاً**: تُحفظ بدلو `plan-sources` الخاص (غير
  عام) وصفها بـ`upload_sources`، والبنود تشير لها بـ`source_id`. «عرض
  المصدر» بتفاصيل الواجب وبشاشة المراجعة يفتحها برابط موقّع ٥ دقائق من
  `/api/sources/[id]` بعد التحقق من الملكية. التنظيف بكرون التذكيرات
  اليومي (`purgeExpiredSources`): تُحذف الملفات ثم الصفوف، والبند يبقى
  ويصير `source_id` فيه `null`. فشل حفظ الصورة **ما يوقف الرفعة** —
  المصدر ميزة مساعدة، والبديل خسارة تحليل ناجح. (قرارها: ٥ أيام لأسبوع
  كافية — اخترنا ٧.)
- **تنبيه الطالب/ة الغلط**: لو الصورة مكتوب عليها صف يخالف صف الطالب/ة
  المختار، يطلع تنبيه أحمر بأعلى شاشة المراجعة (`gradeMismatch`). شوهد
  بالإنتاج ١٨ سبتمبر: رُفعت خطة الصف الرابع على طالب بالصف السادس وما طلع
  أي تنبيه. **تنبيه فقط لا منع** — قد ترفع الأم خطة مشتركة، والمقارنة
  تصير فقط لو الصف مكتوب صراحةً بالصورة (الغائب ما يولّد تحذيراً كاذباً).
- **الحفظ المنظّم**: `surah` و`from_ayah` و`to_ayah` و`recite_on` جنب
  `reference` الحرفي لا بدلاً عنه، وكلها تبقى null لو مو مكتوبة بالصورة.
  موعد التسميع ما ياخذ رجوع «آخر يوم بالخطة» اللي للمهام — بلا موعد
  مكتوب يبقى null، وإلا صار لكل حفظ موعد ما ذكرته المدرسة.
- **ربط المستلزم بمهمته**: `requirements.task_id` يُملأ فقط لو مادة
  المستلزم عندها **مهمة وحدة بالضبط** بنفس الرفعة؛ أكثر من وحدة = ربط
  غامض فيبقى null. لا ربط بالتخمين.
- **تعديل الأم يتقدّم**: `edited_by_user` على `tasks` و`requirements`
  و`memorization` — يُرفع بأي تعديل يدوي (مسارات PATCH أو تعديل بشاشة
  المراجعة)، و`applyPlanItems` ما تكتب فوق صف مرفوع علمه. قبل هذا كانت
  إعادة رفع نفس الخطة تمسح تصحيح الأم وترجّع نص النموذج.
- بعد القراءة الأولى يمرّ مرور تحقق ثانٍ (`<feature>_verify`) يراجع الناتج
  خليةً خلية على الصورة. لا تمرَّر `temperature` — النموذج يرفض الطلب بها.
- الرفعة تطول ٤٥–٦٠ ثانية والجهاز يقطع الطلب قبلها (قفل الشاشة، شبكة
  ضعيفة) فيظهر «Load failed» رغم أن الخادم أكمل وحفظ. لذلك يرسل الجهاز
  `jobId` مع الصور، والخادم يسجّل الرد النهائي في `upload_jobs`
  (`lib/uploadJobs.js`)، والجهاز يستعلم عنه من `/api/upload-jobs/[id]` لو
  انقطع الاتصال (`lib/uploadRequest.js`). أي مسار رفع جديد لازم يمر بنفس
  الطريقة.

## الدخول: موبايل + رقم سري، بلا كود (قرار ١٩ سبتمبر)

قرار ١٦ سبتمبر كان: كود إلزامي مرة واحدة لكل رقم، ثم شاشة **اختيارية**
لتحديد رقم سري. النتيجة بالإنتاج: **١٢٦ من ١٥٨ حساباً (٨٠٪) تخطّوا
الشاشة**، فصار يوصلهم كود بكل دخول — وهذا مصدر فاتورة Twilio كلها. قرار
١٩ سبتمبر ألغى الكود من مسار الدخول والتسجيل نهائياً:

- **صفحة إنشاء حساب**: الاسم + الموبايل + رقم سري ← `/api/auth/signup`،
  بلا أي كود. الرقم يُحفظ كما هو بلا تحقق منه: نحتاجه لدعوة ولي الأمر
  الثاني وللدفع، لا لإثبات الهوية.
- **صفحة تسجيل دخول**: الموبايل + الرقم السري ← `/api/auth/login-password`.
- الشاشتان منفصلتان مع رابط تبديل بينهما، والافتراضية هي الدخول.

**الحارس الوحيد اللي يمنع سرقة الحسابات**: `signup` يرفض بـ409 أي رقم
**مسجَّل** ولا يكتب عليه شيئاً. بدونه يقدر أي شخص يكتب رقم أم مسجّلة
ويحدد رقماً سرياً ويدخل بيانات عيالها — وهي نفس الثغرة اللي انحذف بسببها
مسار `/api/register` قبل. **أي تعديل يخلي التسجيل يكتب على رقم موجود
يعيد فتح الثغرة.**

**الكود بقي بمكان واحد فقط: «نسيت الرقم السري»** (`bypassPassword:true`).
بدون أي قناة تحقق يقدر أي شخص يعيد تعيين رقم سري لأي حساب — فالاستغناء
عنه هنا يلغي الأمان كله. وهو نادر، فتكلفته لا تُذكر.
وبعد نجاح الكود تُجبر الأم على اختيار رقم سري جديد: `verify-otp` يرجّع
`needsPassword:false` لمن عندها رقم سري أصلاً، فالواجهة تحمل علم `forgot`
لأن غيابه كان يخلّيها تدخل بالكود وتطلع بلا ما تغيّر رقمها المنسي.

**المشتركات القديمات بلا رقم سري**: `dashboard` يرجّع `needsPassword`،
والتطبيق يعرض لهن شاشة **إلزامية** «أكملي حسابك» أول ما يفتح. جلستهن
السارية هي إثبات الهوية، فيحدّدن الرقم السري عبر `/api/set-password`
**بلا كود ولا تكلفة**. هذا اللي يقلّص عدد من يحتاج «نسيت الرقم السري».

الحماية من التخمين كما هي: `mothers.failed_login_attempts`/
`login_locked_until` (`lib/authPassword.js`) — ٥ محاولات غلط تقفل الدخول
بالرقم السري ١٥ دقيقة، و«نسيت الرقم السري» يفكّ القفل بعد كود ناجح.
و`password_hash` بصيغة scrypt يدوي (`salt:hash`) بلا تبعية جديدة.

## قاعدة البيانات: الوصول من الخادم فقط

التطبيق ما يستخدم مفتاح `anon` أبداً — كل الوصول من مسارات الخادم بمفتاح
`service_role` (`lib/supabase.js`) الذي يتجاوز RLS. لذلك **كل جدول جديد
يُنشأ بـ`enable row level security` بلا سياسات** (يقفل أي وصول مباشر عبر
REST ولا يغيّر سلوك التطبيق)، وأي دالة `SECURITY DEFINER` تُقصر على
`service_role` (`revoke execute ... from public, anon, authenticated`).
تنبيه Supabase ١٣ سبتمبر ٢٠٢٦ كان بسبب خمسة جداول بلا RLS — عولج.

## تواصل

كل ما يظهر للمستخدمة بالعربي الكويتي/الخليجي. وكذلك التواصل مع صاحبة
البرنامج.

قناة التواصل بالتطبيق هي **المسجات الدايركت بالانستقرام**
(`INSTAGRAM_URL = https://ig.me/m/reemora.app` بـ`app/page.jsx`) بدل
الايميل — قرار صاحبة التطبيق ١٨ سبتمبر. الواتساب باقٍ كما هو. والايميل
يبقى مكتوباً بصفحتي **الخصوصية والشروط** فقط، كقناة رسمية لطلبات حذف
أو تصدير البيانات.
